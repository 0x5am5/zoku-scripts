#!/usr/bin/env bash
# Blocks a commit when the committed bundles are stale (a module was edited but
# ./build.sh wasn't re-run). Enabled via: git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
./build.sh --check
